import React from "react";
import { View } from "react-native";

export default function Home(): React.ReactElement {
  // Wrapper כבר לא בשימוש. נשמר רכיב ריק כדי להימנע משבירה אם יש שימושים עתיקים.
  return <View style={{ flex: 1 }} />;
}
